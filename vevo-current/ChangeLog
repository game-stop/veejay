
1.0
	- initial public release
1.1
	- added library functions:

		int	vevo_init_parameter_values(
				vevo_port *p,
				int p_args,
				vevo_atom_type_t ident,
				void *val,
				int n_types,
				... );

		int	vevo_property_assign_value(
				vevo_port *p,
				vevo_property_type_t left,
				vevo_property_type_t right );	

		int	vevo_property_assign_value_from(
				vevo_port *p,
				vevo_property_type_t left,
				vevo_atom_type_t ident, void *data );	

	- updated documentation
	+ added doxygen configuration file
	+ header cleanup
	+ new example plugin 'rgbkey'
